# Audio (MP3)

Place short MP3 previews here (e.g. `preview-track.mp3`). In `index.html`, set the `<audio>` element’s `src` to match your filename.

GitHub Pages serves static files as-is; very large MP3s will slow the first visit. Prefer clips under a few megabytes, or link out to streaming platforms instead.
